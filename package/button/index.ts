import JmButton from './button.vue';

export enum Type{
    primary = 'primary',
    info = 'info',
    default = 'default',
    error = 'error',
    warning = 'warning',
    success = 'success'
}

export enum Size{
    normal = 'normal',
    small = 'small',
    large = 'large'
}

export default JmButton;